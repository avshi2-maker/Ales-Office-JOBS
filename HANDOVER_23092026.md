# Ales Office JOBS + Case Pipeline — Handover
**Date:** 23.09.2026 · **Owner:** Avshi Sapir (avshi2-maker) · **Supersedes:** HANDOVER_22092026.md (kept for history)

Plan & mockups (artifact): "Marble Art Case Engine" — Ales field job → CRM case study → indexed marble-art.co.il page.

---

## 1. DONE TODAY (all pushed, Vercel auto-deploys)

### A. Go-live — ales.marble-art.co.il ✅
- Vercel build fix: `vercel.json` framework=nextjs + next 14.2.5 → 14.2.35 (security). `6dc8713`
- Supabase NEW project `oqqviohkynhmbfsqntxr`: `supabase.sql` run, table + RLS verified.
- Cloudinary `dqdku88vv` (Marble Art account): preset `ales_unsigned` (Unsigned, folder `ales-jobs`).
- Vercel: 5 `NEXT_PUBLIC_` env vars, clean redeploy, values verified in live bundle.
- DNS (cPanel Zone Editor): CNAME `ales` → `e68784d71992a838.vercel-dns-016.com.` + TXT `_vercel` (ownership). Vercel = Valid Configuration.
- Avshi field-tested on phone: all OK.

### B. Clock + date stamps ✅ `1c78b06`
- Live clock + Hebrew weekday/date in header (Israel time) — `LiveClock.js`
- Date stamp on capture form (`LiveStamp.js`), every gallery card, and WhatsApp share text — `lib/datestamp.js`

### C. Phase 1 — finish chapter + testimonial on every job ✅ `7fb3933`
- New page `/job/[id]`: job summary → **✅ סיום עבודה + המלצה**: after photos (≥1 required, tip for 4+), finish date, customer first name, ★ rating, quote, voice note, 3 consents (photos / first name+city / quote).
- Gallery: status badge 🛠️ בביצוע / ✅ הסתיים + open/finish button; finished jobs show after-media + quote.
- DB: `supabase_phase1_finish.sql` (APPLIED): columns `status, finished_at, finish_date, after_media, testimonial, consent` + rpc `finish_job` (security definer: open jobs only, finish columns only, ≥1 photo). Direct PATCH from browser = 0 rows (verified).
- Share text: **price removed**; quote + after photos only when consented. Price still visible in-app, labelled "פנימי".

### D. CRM — 🔎 אינדוקס גוגל (`/index-tracker`) ✅ Sinks_ART `c59a091`
- Reads the LIVE sitemap of marble-art.co.il on every open → every public URL auto-registered in `index_log` (table APPLIED on `givcxgzhfoetujhrjgvc`).
- Per URL: 📋 copy · 🔍 Search Console URL-inspection · 🌐 `site:` check · 📨 mark sent · ✅ mark indexed · ↺ reset. Bulk copy / bulk mark. Filters by status + page type.
- Nav: כסף ונתונים → 🔎 אינדוקס גוגל.
- Search Console link assumes property `sc-domain:marble-art.co.il`. If it errors → set Vercel env `GSC_PROPERTY=https://www.marble-art.co.il/` on the CRM project.
- Daily routine: 🆕 חדשים → Search Console "Request indexing" → 📨 סמן נשלח (~10/day Google limit, projects first).

### Wiring changed today
- Sinks_ART: origin now carries the same GitHub PAT → **Claude auto-pushes the CRM too**. Repo-local git identity set (avshi2-maker). Delete permission granted this session (git locks).
- Avshi's uncommitted CRM edits (README, SKILL.md, IDEAS_PARKING, etc.) were **left untouched**.

---

## 2. TODO AFTER BREAK — in this order

### ▶ Task 1 (NEW, Avshi 23.09) — Mobile capture buttons: shoot / record / upload, save-now-send-later
**Why:** today's inputs use `capture="environment"` → on many phones that forces the camera and HIDES the gallery option; and voice is upload-only (no in-app recorder).

Build in Ales app (used by BOTH new-job form and finish chapter):
1. **Media bar — 3 explicit big buttons:**
   - 📷 **צלם תמונה** → `<input accept="image/*" capture="environment">`
   - 🎥 **צלם וידאו** → `<input accept="video/*" capture="environment">`
   - 🖼️ **העלה מהגלריה** → `<input accept="image/*,video/*" multiple>` (NO capture attr → phone shows gallery/files)
2. **🎙️ In-app recorder for the happy-customer conversation:**
   - Big ● הקלט / ■ עצור button, live timer, playback before saving (MediaRecorder API; iPhone Safari records `audio/mp4`, Android `audio/webm` — accept both).
   - Keep the existing "upload audio file" option next to it.
3. **Save now, send later (offline queue):**
   - Every photo/video/recording is saved first to the phone (IndexedDB) with the job id, shown as "⏳ ממתין להעלאה".
   - Auto-uploads to Cloudinary when online; manual "⬆️ שלח עכשיו" button; badge in header with pending count.
   - Survives closing the browser; nothing lost on a building site with no signal.
4. **Hand-off to CRM transcription (already exists):** CRM `POST /api/sinc-transcribe` takes `{ audioUrl, durationSec }` (Cloudinary URL, ElevenLabs Scribe Hebrew) → reuse it. Store `durationSec` with each recording so the CRM can transcribe → save to testimonials. Wired end-to-end in Task 2 (bridge).
- Files (plan): `components/MediaBar.js`, `components/Recorder.js`, `lib/offlineQueue.js` (IndexedDB), `components/PendingBadge.js`; replace inputs in `MediaCapture.js` + `VoiceNote.js`.
- Effort: MEDIUM. Test on a real iPhone + Android before calling it done.

### ▶ Task 2 — Phase 2: bridge Ales → CRM
- Supabase DB webhook (Ales project) on `status → finished` → CRM `POST /api/ales-intake` with secret header (`ALES_WEBHOOK_SECRET`).
- CRM creates `case_studies` row (status "חדש מאלס"), links/creates customer; if a voice note exists → auto-call `/api/sinc-transcribe` → transcript saved to the testimonial.

### ▶ Task 3 — Phase 3: CRM module `/case-studies`
- Inbox → ✨ Generate (Claude, Ales facts only) → 10 auto gates → edit → ✅ Approve & publish. Must include ApiCostMeter + ExportFooter (5 buttons).

### ▶ Task 4 — Phase 4: public site reads published cases
- `/projects/[slug]` merges static `projects.ts` + `case_studies` (published) · JSON-LD (Article/Review/FAQ/Breadcrumb/Image) · sitemap real lastModified · city-page project block · dynamic `llms.txt` · IndexNow ping.
- New case URLs then appear automatically in 🔎 אינדוקס גוגל.

### Later — Phase 5 (distribution: Content Studio, GBP post, Google-review WhatsApp) · Phase 6 (per-case leads in /roi).

---

## 3. Known limits / notes
- PIN gate is a door latch, not a lock: `NEXT_PUBLIC_APP_PIN` is in the public JS. Real protection = narrow DB rules (insert + `finish_job` only). Fine for now; revisit if the app goes beyond Ales.
- Customer phone deliberately NOT stored in the Ales app (anon key = readable). CRM handles phones.
- Review stars from own-site reviews won't show in Google — stars come from Google Business Profile (Phase 5 review ask).

## 4. Resume checklist (next session)
1. Read this file.
2. Folders connected: `C:\Ales-Office-JOBS`, `C:\SinkS\Sinks_ART`, `C:\SinkS\sinks-bathroom-design`. Token in each `.git/config` origin.
3. Request delete permission for any repo git touches (lock cleanup).
4. Build-test: Ales in `$HOME/alesbuild` (npm ok on device). CRM in `$HOME/crmbuild` — device VM can't reach Google Fonts → stub `next/font/google` in the throwaway copy only.
5. Start Task 1.
